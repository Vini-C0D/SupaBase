-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.usuarios (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nome text NOT NULL,
  email text NOT NULL UNIQUE,
  CONSTRAINT usuarios_pkey PRIMARY KEY (id)
);
CREATE TABLE public.perfis (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  usuario_id bigint UNIQUE,
  bio text,
  avatar_url text,
  CONSTRAINT perfis_pkey PRIMARY KEY (id),
  CONSTRAINT fk_usuario FOREIGN KEY (usuario_id) REFERENCES public.usuarios(id)
);
CREATE TABLE public.turmas (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nome text NOT NULL,
  CONSTRAINT turmas_pkey PRIMARY KEY (id)
);
CREATE TABLE public.alunos (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nome text NOT NULL,
  turma_id bigint,
  CONSTRAINT alunos_pkey PRIMARY KEY (id),
  CONSTRAINT fk_turma FOREIGN KEY (turma_id) REFERENCES public.turmas(id)
);
CREATE TABLE public.disciplinas (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nome text,
  CONSTRAINT disciplinas_pkey PRIMARY KEY (id)
);
CREATE TABLE public.alunos_disciplinas (
  aluno_id bigint NOT NULL,
  disciplina_id bigint NOT NULL,
  CONSTRAINT alunos_disciplinas_pkey PRIMARY KEY (aluno_id, disciplina_id),
  CONSTRAINT alunos_disciplinas_aluno_id_fkey FOREIGN KEY (aluno_id) REFERENCES public.alunos(id),
  CONSTRAINT alunos_disciplinas_disciplina_id_fkey FOREIGN KEY (disciplina_id) REFERENCES public.disciplinas(id)
);
CREATE TABLE public.alunoss (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nome text,
  CONSTRAINT alunoss_pkey PRIMARY KEY (id)
);
CREATE TABLE public.disciplinass (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nome text,
  CONSTRAINT disciplinass_pkey PRIMARY KEY (id)
);
CREATE TABLE public.alunos_disciplinass (
  aluno_id bigint NOT NULL,
  disciplina_id bigint NOT NULL,
  CONSTRAINT alunos_disciplinass_pkey PRIMARY KEY (aluno_id, disciplina_id),
  CONSTRAINT alunos_disciplinass_aluno_id_fkey FOREIGN KEY (aluno_id) REFERENCES public.alunoss(id),
  CONSTRAINT alunos_disciplinass_disciplina_id_fkey FOREIGN KEY (disciplina_id) REFERENCES public.disciplinass(id)
);